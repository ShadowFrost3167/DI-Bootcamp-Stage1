// Write as comments the steps of the resolution of this piece of code

// Guess what will be the result before checking it



let a = 2 + 2;

switch (a) 
  case 3:
    alert( 'Too small' );
    break;
  case 4:
    alert( 'Exactly!' );
    break;
  case 5:
    alert( 'Too large' );
    break;
  default:
    alert( "I don't know such values" );



//===================================================

// let userInput = prompt("What is 2+2?");
    //make the user input a value to a prompt of the question. 
// let num = parseFloat(userInput);
    //switch the user input to being calculated as a number.

// switch (num) {
    //change the switch from a to the num since you want the value of the input the user makes to be analyzed and responded to by the page. 
//   case 3:
//     alert( 'Too small' );
//     break;
//     //in the case where a=3 (input from user is 3) pop up message as alert will say 'Too Small' break is end of that case
//   case 4:
//     alert( 'Exactly!' );
//     break;
//     //in the case of user inputing 4 alert will say exactly break ends that case
//   case 5:
//     alert( 'Too large' );
//     break;
//     //in the case where user inputs 5 alert says too large break ends that case
//   default:
//     alert( "I don't know such values" );
//     //in case of any other inputs i don't know such values
// }